LMS_API_URL=https://claude-lms.devmanag.workers.dev

# Local dev only (`next dev`, seed scripts). Production uses Cloudflare Worker secrets.
# Must be the API key for tenant `lms_demo` (not `edumio`).
LMS_API_KEY=your_lms_demo_api_key_here
LMS_TENANT_KEY_PREFIX=7a4e5216

DEMO_ADMIN_PASSWORD=your_admin_password
